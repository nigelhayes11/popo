name: 🚀 Update Live Events Playlist 📺

on:
  schedule:
    - cron: '*/10 * * * *'   # ⏰ Every 10 minutes (UTC)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests

      - name: 🎯 Run live events filter
        run: |
          python3 liveeventsfilter.py liveevents.m3u8 liveeventsfilter.m3u8

      - name: 💾 Commit & Push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@users.noreply.github.com"

          git add liveeventsfilter.m3u8

          if git diff --cached --quiet; then
            echo "✅ No changes detected"
            exit 0
          fi

          git commit -m "🔁 Update liveeventsfilter.m3u8 ($(date -u))"
          git push
